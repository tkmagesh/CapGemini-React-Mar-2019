import addNew from './addNew';
import toggle from './toggle';
import removeClosed  from './removeClosed';


const bugActionCreators = { addNew, toggle, removeClosed };

export default bugActionCreators;